# Universal prompt

Use this only when an AI agent cannot discover `SKILL.md`, `AGENTS.md`,
`CLAUDE.md`, `GEMINI.md`, or Cursor project rules by itself.

```text
Use Zede-Zero for this request.

First, read zede-zero-learning-roadmap/SKILL.md completely. Treat it as the
single canonical workflow. Resolve every referenced file relative to the
zede-zero-learning-roadmap directory and load it only when that workflow says
to. Follow its first-use explanation, one-question-at-a-time clarification,
goal-card confirmation, official-source research, output-mode selection,
verification, save-consent, feedback, and tutoring-boundary rules.

Do not invent missing facts or claim that a source was checked when it was not.
Do not modify the Skill unless I explicitly request a permanent Skill change.

My request: [replace this line with your learning-roadmap request]
```
